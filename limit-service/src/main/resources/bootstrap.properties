# configuring application name
spring.application.name=limit-service

#configuring an url to access spring cloud config server
spring.cloud.config.uri=http://localhost:8888

#to indicate which property file should be given property we can configures below
spring.profiles.active=dev